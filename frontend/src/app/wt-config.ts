/** Généré / mis à jour par backend/utils/generate-cert.sh — valeurs par défaut pour le local. */
export const WT_PORT = 3003;
export const WT_HASH_BASE64 = '';
/** IP recommandée (WSL eth0 ou LAN). Ouvrir l’app via http://${WT_SETUP_HOST}:4200 depuis Chrome. */
export const WT_SETUP_HOST = '127.0.0.1';
