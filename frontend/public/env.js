// Remplacé en production (Vercel / Docker). En local : laisser vide → localhost + ports.
window.__ENV__ = window.__ENV__ || {
  // WS_URL: 'ws://localhost:3001',
  // SSE_URL: 'http://localhost:3002',
  // WT_URL: 'https://127.0.0.1:3003',
};
